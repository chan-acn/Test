
// Correct camelCase
let userName = "Alice";
const totalCount = 10;

// Incorrect (snake_case)
let user_name = "Bob";

// Incorrect (PascalCase)
let UserName = "Charlie";

// Incorrect (all caps)
const TOTAL_COUNT = 20;
